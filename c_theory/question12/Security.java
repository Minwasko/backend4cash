package c_theory.question12;

public class Security {

    //todo A
    // What is authentication?
    // Answer: It is a process of ensuring that something(host machine, token)/someone(user) is genuine

    //todo B
    // What is authorization?
    // Answer: process of giving access rights to someone, if they meet needed criteria

    //todo C
    // For web (HTTP backend) Spring Security is configured as a ... ? Explain it.
    // Think or read how Spring Security checks are injection into your application.
    // Configured as (one word): filter
    // Description: it acts as a filter, which filters out incoming requests based on access rights that they have

}
