package a_theory.question1;

public class Compilation {

    //todo
    // Sometimes things get complicated, sometimes things get compiled.

    //todo p1
    // In your words (do not use wiki definitions)
    // What is compilation?
    //Answer: a compilation is a process of transforming humanly readable code to machine code.

    //todo p2
    // Name 2 benefits of compilation?
    //1 Once compiled, the program does not require the libraries that programmer used installed
    //2 Compiled code is generally faster than interpreted code, because the machine does not need to transform it
    // to machine code

    //todo p3
    // Name 1 disadvantage of compilation?
    //1 You have to wait for the thing to compile. Sometimes it's a long process.
}
